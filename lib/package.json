{
  "name": "lib",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "//build": "Builds the page bundles into lib/dist: lobby.js (both lobby pages, which load lobby-evm.js or lobby-tvm.js at runtime) and main_game.js (both match pages). lib/dist is generated and gitignored - run this after any change under lib/ and before deploying. From the repo root: npm --prefix lib ci && npm --prefix lib run build",
    "build": "webpack",
    "build:clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\" && webpack",
    "watch": "webpack --watch",
    "dev": "NODE_ENV=development webpack --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@oasisprotocol/sapphire-paratime": "1.1.1",
    "@ton/ton": "^14.0.0",
    "@tonconnect/sdk": "^3.0.5",
    "@tonconnect/ui": "^2.0.8",
    "@walletconnect/ethereum-provider": "2.10.1",
    "@walletconnect/modal": "^2.6.2",
    "buffer": "^6.0.3",
    "ethcall": "^6.0.0"
  },
  "devDependencies": {
    "webpack": "^5.76.3",
    "webpack-cli": "^5.0.1"
  }
}
